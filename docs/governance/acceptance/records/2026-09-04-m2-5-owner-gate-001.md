---
acceptanceId: M2-5-OWNER-GATE-001
subject: "M2.5 Traceability Verification Snapshot Implementation Candidate"
subjectCommit: 451b0950b279cd819ed7efe8ab76d1b7b49e8ef7
pairedSubjectCommit: N/A
branch: "docs/m2-issue-traceability-design"
status: PENDING
submittedAt: 2026-09-04T22:33:35Z
owner: PENDING
decisionAt: PENDING
---

# M2.5 Traceability Verification Snapshot 验收候选记录

## Scope

**WHY**

M2.3 已固定 Release 的 Issue 集合，M2.4 已固定三类 typed Edge Revision。M2.5 候选需要证明系统能够只使用这些历史事实和 Locked Manifest authority，确定性生成可审计、可重放的 Fixed、Included、固定 `Verified=false` 与精确 Gap，并在故障后恢复而不改写历史。

**WHAT**

- 候选 Subject Commit `451b0950b279cd819ed7efe8ab76d1b7b49e8ef7` 上的 V11 Migration、异步 Verification Run、固定 Input Ledger、Worker、不可变 Snapshot、read/replay API，以及 Task 7 性能、恢复、Gate、CI 和运维规范。
- 20 个 Issue、2,000 条固定 Edge 的 Pilot 性能与常数级 query-count 验证。
- PostgreSQL `pg_dump` 到独立 PostgreSQL `pg_restore` 后，从恢复关系事实重算 Snapshot canonical digest；以严格更新的 `pg_postmaster_start_time()` 证明独立数据库真实 restart，再通过 fresh reconnect/repository reclaim RUNNING job；另覆盖 poison Dead Letter 与新 Run 人工 retry。
- 固定 12 步 fail-closed Gate、Evidence exact-property allowlist/secret 与绝对路径拒绝、summary/digest 和只读 GitHub Actions Artifact。

**BOUNDARY**

- 不改变 V0.1 Core Contract、Release-centric architecture、Locked Manifest authority、Evidence、Traceability、Deterministic Quality Engine、Adapter/Plugin 或 ADR 治理。
- M2.5 不读取 Jira、GitHub、CI、Device、Test Agent 或 Company 环境，不产生 `Verified=true`，不从最新 Revision、JSON、文件或缓存重建历史。
- 不授权 Company、真实 Provider、M3、`main`/`release` merge、Tag、release 或 production deployment。
- 共享 CI 的宽松性能硬上限只防算法退化，不代表 Company 性能承诺或 Company Ready。

**ACCEPTANCE**

Project Owner 只有在双语 Subject 固定、两个 exact-head Linux/Docker CI 均成功、两份 `m2-5-evidence-${commit}` Artifact 可用、Pair Gate 通过，并复核 V11、完整链/五类 Gap、transaction、concurrency、replay、recovery、security 与 20/2,000 性能 Evidence 后，才可给出 `APPROVE`、`CONDITIONAL` 或 `REJECT`。当前记录不代替 Owner 决策。

## Evidence

- **候选 Git Evidence**：中文 Subject Commit `451b0950b279cd819ed7efe8ab76d1b7b49e8ef7`，Subject `test(m2): add traceability verification candidate gate`；Availability `LOCAL_ONLY`；Owner Authorization：`UNKNOWN`。
- **本地 Gate Orchestration**：`scripts/tests/m2-5-verify-gates.tests.ps1` 为 `PASS`；验证固定顺序、真实 Gradle stub exit `23` 后继续到 acceptance、dirty/exact-head fail-closed、额外 secret/Windows/Unix 绝对路径拒绝与清理、脱敏 JSON 与 SHA-256 sidecar；Owner Authorization：`UNKNOWN`。
- **本地 Compile/Contract/Governance**：Kotlin production/test compile 为 `BUILD SUCCESSFUL`；Contract 为 `schemas=4 positive=12 negative=5 operations=34`；既有 Acceptance Validator 为 `PASS`；Owner Authorization：`UNKNOWN`。
- **本地 PostgreSQL 性能/恢复**：`BLOCKED`；本机 Testcontainers 在 `DockerClientProviderStrategy` 初始化阶段失败，2 个目标测试未执行 fixture、SQL、事务、性能或恢复断言，不计为 PASS；Owner Authorization：`UNKNOWN`。
- **双语 exact-head CI、Artifacts 与 Pair Gate**：`PENDING`；等待英文配对 Subject、双分支推送和 Linux/Docker CI，不声称 Company Ready；Owner Authorization：`UNKNOWN`。

## Acceptance Checks

| Check | Result | Evidence | Notes |
|---|---|---|---|
| V0.1/V0.2 冻结边界 | `PASS` | Candidate diff 与运维边界 | 没有生产语义、Migration 或 authority 修改 |
| Gate orchestration 与脱敏 Evidence | `PASS` | 本地 Gate fixture test | 12 步固定顺序；真实 child exit 23 后继续；任一失败保持总 FAILED 并生成 summary/digest；拒绝非 allowlist/secret/绝对路径子报告 |
| Kotlin compile、Contract 与治理 | `PASS` | 本地 fresh commands | Contract operations `34`；Owner 状态保持 PENDING |
| 20 Issue/2,000 Edge 性能 | `PENDING` | exact-head CI required | 本机 Docker 不可用；不得缩小 fixture 或以 compile 代替运行 |
| Snapshot 恢复、reclaim 与 Dead Letter | `PENDING` | exact-head CI required | 源码已使用独立 PostgreSQL dump/restore，并以 restart 前后 postmaster start time 严格更新及 fresh reconnect 证明进程边界；本机 Docker 不可用，运行断言仍待 CI |
| 双语 Pair Gate 与 exact-head Artifacts | `PENDING` | English sync / CI required | 当前 paired Subject 尚未固定 |
| Company readiness | `N/A` | Scope Excluded | 当前候选仅覆盖 Pilot implementation authority |
| Owner Decision | `PENDING` | 本记录 | Codex 不代替 Project Owner 决策 |

## Residual Risks

| Risk | Impact | Owner | Mitigation / Review Condition |
|---|---|---|---|
| 本机没有 Docker/Testcontainers runtime | 尚无当前 Task 7 的真实 PostgreSQL 性能和恢复结果 | Implementation Owner | 在双语 exact-head Linux/Docker CI 执行完整 Gate，并保存两个 Artifacts |
| 固定参考硬件尚未建立 | 共享 CI 数据不能承诺目标 P95 或 Company 性能 | Project Owner / Implementation Owner | 保留目标与宽松硬上限的区分；Company 前在固定环境独立验收 |
| 英文配对 Subject 尚未创建 | 当前不是双语完整候选 | Implementation Owner | 翻译 Markdown、保持非 Markdown byte-identical，并通过 Pair Gate |
| GitHub Artifact 有 30 天保留期 | 在线 Evidence 后续可能不可访问 | Release Engineer / Project Owner | Owner 复核后按 Evidence Archive 治理做独立归档 |
| Git commit 不是身份签名 | locator 不能独立证明操作者身份 | Project Owner / Release Engineer | 保留 Owner 原始指令与后续双语 receipt；范围扩展单独授权 |

## Decision Reason

PENDING

## Follow-up Actions

| Action | Owner | Due / Trigger | Closure Condition | Completion Evidence |
|---|---|---|---|---|
| 创建英文配对候选并执行 Pair Gate | Implementation Owner | 中文 record commit 后 | 英文 Markdown 零汉字、非 Markdown byte-identical、Pair Gate PASS | 双语 commits 与 Pair Gate report |
| 执行双语 exact-head M2.5 CI | Implementation Owner | 双语推送后 | 两条 M2 Backend runs success，均生成完整 Artifact | Run/Job/Artifact IDs 与 digest |
| 复核并决定 M2.5 Owner Gate | Project Owner | 上述 Evidence 完整后 | 明确给出 APPROVE、CONDITIONAL 或 REJECT | Owner 原始指令与独立 receipt commit |
| 保持排除范围关闭 | Project Owner / Release Engineer | 独立授权前 | 不启用 Company/真实 Provider，不 merge/Tag/release/deploy | 配置、Git 与发布审计记录 |

## Decision History

| At | Status | Owner | Reason | Commit |
|---|---|---|---|---|
| 2026-09-04T22:33:35Z | PENDING | PENDING | 固定中文 M2.5 implementation candidate、本地 Gate/compile/contract Evidence 与明确 Docker/双语/CI 缺口，提交 Project Owner 后续复核 | PENDING |
